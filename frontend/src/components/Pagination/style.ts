import { styled } from 'styled-components';

const StyledPaginationComponent = styled.section`
  .pagination__button__disabled {
    pointer-events: none;
    cursor: none;
  }
`;
export default StyledPaginationComponent;
